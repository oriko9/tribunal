{
  "name": "tribunal",
  "version": "0.1.0",
  "private": true,
  "description": "The Tribunal — seven agents deliberate one fixed case and return three independent opinions.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "tribunal": "node --env-file-if-exists=.env.local --import tsx src/cli.ts",
    "smoke": "node --env-file-if-exists=.env.local --import tsx src/smoke.ts",
    "models": "node --env-file-if-exists=.env.local --import tsx src/models.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vercel/node": "^12.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
